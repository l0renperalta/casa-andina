const express = require('express');
const router = express.Router();

router.get('/', (req, res) => {
  res.json('aws-sdk location service');
});

module.exports = router;

// `Implementación de un sistema de inventario basado en contratos inteligentes utilizando la tecnología
// blockchain para prevenir la manipulación de datos en la gestión de la municipal de Huambo`;

// `Implementación de tecnología blockchain para evitar la manipulacion de datos en la gestión de recursos
// de la municipalidad de Huambo`;
